# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Parsing
PyMuPDF>=1.25.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Embeddings (ONNX via fastembed — no PyTorch, keeps the image small)
fastembed>=0.4.0

# Vector DB
sqlite-vec>=0.1.6

# Graph DB — PIN exact version: the committed data/graphdb file is written in
# kuzu 0.11.3's storage format and a different version cannot read it.
kuzu==0.11.3

# Data processing
numpy>=1.26.0
pandas>=2.0.0          # required by kuzu's get_as_df() (queries + stats)
pyarrow>=15.0.0        # kuzu's get_as_df() can route results through Arrow
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM clients
groq>=0.11.0
openai>=1.50.0
google-genai>=1.0.0
