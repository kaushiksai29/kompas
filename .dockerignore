.venv
node_modules
frontend
**/__pycache__
.git
.next
*.log
# The committed graph/vector DBs are platform-specific (Kùzu files are not
# cross-OS portable). Keep them out of the image — it rebuilds them natively
# from the corpus PDFs + the extraction cache (see Dockerfile).
data/graphdb
data/graphdb.wal
data/vectorstore.db
